// Main idea of JS
   - Save the data
   - Generate the html
   - Make it interactive


// Use objects and arrays to organise data(data structure)


//.toFixed(2) converts a number into a string to a number of decimal places


// Scripts elements are loaded one by one, so order of file links is important


// Data attribut is just another html attribute that allows us to attach any information to an element(uses kebab case)


// Not using modules can cause naming conflicts
   _How to create a module_
    - Create a file
    - Don't load file with script tag
    - all imported variables should be declared at the top
    - has to be online to work(live server) 
    **file that variables get imported to should have their html tag have an attr of type 'module'


// A group of html radio selectors with the same name will give options for the group(so different names for groups under different products)



// Common Jasmine hooks
   - beforeEach() = runs code before tests(setup code or code shared b/n tests can be written here)
   - afterEach() = code like cleanup code to clean up html after a test can be written here
   - beforeAll()
   - afterAll()



// dayJS external date library
   - const today = dayjs();
   - const deliveryDate = today.add(7, 'days');
   - deliveryDate.format('dddd, MMMM, D');





Last 21:37:00

20:27 (asynchronous code explanation)









///REMEMBER
// cart.JS
    // // my code block (Ex)
    // let selectValue;
    // document.querySelectorAll('.js_select_button').forEach((selectButton) => {
    //     selectButton.addEventListener('click', () => {
    //         // console.log(selectButton.value);
    //         // console.log(selectButton.productId);
    //         const productId = selectButton.dataset.productId;
    //         selectValue = selectButton.value;
    //         console.log(selectValue);


    //         // get option value here
    //         // then add value to quantity value. so when 'add to cart' is clicked, it updates that value
            
    //     });
    // });



"&amp;"??
//do exercise 15h (github)
//do exercise 15m
//exercise 16n
//exercise 17i(do this in finished amazon project)


// const someth = someth1 === someth2; //how??
